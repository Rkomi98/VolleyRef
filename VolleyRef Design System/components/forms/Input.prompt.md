Single-line text field with optional leading icon.

```jsx
<Input placeholder="Cerca partita…" icon={<SearchIcon />} value={q} onChange={setQ} />
```

Set `error` to switch the border/focus ring to danger red.
