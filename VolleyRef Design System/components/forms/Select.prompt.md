Native-backed dropdown with brand chevron, styled consistently with Input.

```jsx
<Select options={[{ value: 'a', label: 'Squadra A' }, { value: 'b', label: 'Squadra B' }]} value={team} onChange={setTeam} />
```
