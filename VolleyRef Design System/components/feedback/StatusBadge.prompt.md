Semantic analysis-state pill: validated (green), review (amber), inconsistent (red), processing (blue). This is VolleyRef's core trust signal — used on match headers, set tables, and the Controllo panel.

```jsx
<StatusBadge status="review" />
<StatusBadge status="inconsistent">Set 4 incoerente</StatusBadge>
```
