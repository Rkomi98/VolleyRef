Small dot + tooltip flagging OCR/extraction confidence, without covering the UI in percentages. Renders nothing at "high" confidence by default — the absence of a dot is itself the "this is fine" signal.

```jsx
<ConfidenceIndicator level="low" />
```
